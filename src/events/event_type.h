#ifndef EVENT_TYPE_H
#define EVENT_TYPE_H

namespace Events {
  enum EventType {
    MOUSE_CURSOR,
    MOUSE_SCROLL,
    MOUSE_BUTTON,
    CURSOR_ENTER,
    CURSOR_LEAVE,
    KEY_DOWN,
    KEY_UP
  };
}

#endif
